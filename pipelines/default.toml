[metadata]
name = "default"
description = "Standard development pipeline: plan -> develop -> test -> review -> create PR"

[stages]
planning = { role = "planner", priority = 8 }
development = { role = "developer", priority = 6 }
challenge = { role = "challenger", priority = 5 }
testing = { role = "tester", priority = 4 }
reviewing = { role = "reviewer", priority = 3 }
security-review = { role = "security_reviewer", priority = 2, requires_label = "security" }
pr-creation = { role = "pr_creator", priority = 1 }

[pipelines]
feature = ["planning", "development", "challenge", "testing", "reviewing", "security-review", "pr-creation"]
bug = ["development", "challenge", "testing", "reviewing", "pr-creation"]

[routing]
security = { insert_stage = "security-review", after = "reviewing" }
frontend = { prompt_block = "visual_verification" }

[defaults]
max_spawns_per_cycle = 2
poll_interval_seconds = 5
rejection_cooldown_seconds = 60
